apiVersion: apps/v1
kind: Deployment
metadata:
  name: bookinfo-prd-ratings
  namespace: student114-bookinfo-prd
  labels:
    app: bookinfo-prd-ratings
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bookinfo-prd-ratings
  template:
    metadata:
      labels:
        app: bookinfo-prd-ratings
    spec:
      containers:
      - name: bookinfo-prd-ratings
        image: ghcr.io/peemkaka/bookinfo-ratings:dev
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: web-port
          protocol: TCP
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
            scheme: HTTP
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
            scheme: HTTP
        env:
        - name: SERVICE_VERSION
          value: v1
      imagePullSecrets:
      - name: registry-bookinfo